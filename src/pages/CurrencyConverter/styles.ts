import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  main: {
    display: 'flex',
    width: '80vw',
    justifyContent: "space-around"
  },
  secondaryInput: {
    maxWidth: 275,
    width: '100%',
  }
});
